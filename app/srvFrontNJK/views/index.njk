<!DOCTYPE html>
<html lang="pt-BR">

    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css">
        <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css" rel="stylesheet">
        <title>{{ title }}</title>

        <style>
            body,
            html {
                min-height: 100%;
                display: flex;
                flex-direction: column;
            }
            .hero {
                background-color: #9c38ff;
                color: white;
                padding: 60px 0;
                text-align: center;
                margin-bottom: 40px;
            }
            .hero h1 {
                font-size: 3rem;
                font-weight: bold;
            }
            .card {
                box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            }
            .btn-primary {
                background-color: #9c38ff;
                border-color: #9c38ff;
            }
            .btn-primary:hover {
                background-color: transparent;
                border-color: #7f2bcf;
                color: #7f2bcf;
            }
            footer {
                background-color: #f8f9fa;
                padding: 20px 0;
                text-align: center;
                margin-top: auto;
            }
            footer a {
                color: #9c38ff;
            }
        </style>
    </head>

    <body>
        <!-- Hero -->
        <div class="hero">
            <h1>Bem-vindo</h1>
            <p class="lead">Aqui você pode gerenciar a sua escola de maneira eficiente e intuitiva.</p>
        </div>

        <!-- Conteúdo -->
        <div class="container">
            <div class="row">
                <div class="col-md-12">
                    <div class="card mb-4 text-center">
                        <div class="card-body">
                            <i class="bi bi-building fs-1"></i>
                            <h5 class="card-title mt-3">Salas de Aula</h5>
                            <p class="card-text">Gerencie as salas de aula, visualize, edite e adicione novas.</p>
                            <a href="/manutSalaDeAula" class="btn btn-primary">Ir para Manutenção de Salas de Aula</a>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <!-- Footer -->
        <footer class="mt-5">
            <div class="container">
                <p>&copy; 2024 {{ title }}</p>
            </div>
        </footer>
    </body>

</html>